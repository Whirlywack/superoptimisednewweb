import { PostPage } from '@/components/templates/PostPage';

export default function Day1FoundationPage() {
  return <PostPage />;
}

export const metadata = {
  title: 'Why Building in Public Creates Better Products - Superoptimised',
  description: 'Day 1 of building transparently. Why community feedback leads to better products and how every decision will be documented openly.',
};